<section>
  <h2>What Exercise are you doing?</h2>

  <form class="new-post-form">
    <div>
      <label for="post-title">Workout Name</label>
      <input type="text" id="post-title" name="post-title" />
    </div>
    <h2 class="tempName">Please list out your workout data.  Please select at least one criteria and use all that apply.</h2>
    <div>
      <label for="post-distance">Distance (miles)</label>
       <input type="number" value=0 id="post-distance" name="post-distance" />
    </div>
    <div>
      <label for="post-time">How Long in Time (minutes)?</label>
       <input type="number" value=0  id="post-time" name="post-time" />
    </div>
    <div>
      <label for="post-weight">How much weight (lbs)?</label>
       <input type="number" value=0  id="post-weight" name="post-weight" />
    </div>
    <div>
      <label for="post-sets">How many sets?</label>
       <input type="number" value=0  id="post-sets" name="post-sets" />
    </div>
    <div>
      <label for="post-reps">Reps</label>
       <input type="number" value=0  id="post-reps" name="post-reps" />
    </div>
    <button type="submit" class="btn">Create</button>
  </form>
</section>

{{#if posts.length}}
<section>
  <h2>Your Exercises</h2>
  <ol>
    {{#each posts as |post|}}
    <li>
      {{> post-info post}}
      <a href="/dashboard/edit/{{post.id}}" class="edit-link">Edit post</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}

<script src="/javascript/add-post.js"></script>